version: "2"
linters:
  default: standard
  settings:
    errcheck:
      # Output goes to stdout; a failed write there is not actionable.
      exclude-functions:
        - fmt.Fprint
        - fmt.Fprintf
        - fmt.Fprintln
formatters:
  enable:
    - gofmt
